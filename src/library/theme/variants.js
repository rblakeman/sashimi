import { rems } from './';

export const FONT_SIZES = {
    fontSize: {
        small: { fontSize: `${rems('12px')}` },
        medium: { fontSize: `${rems('16px')}` },
        large: { fontSize: `${rems('24px')}` },
        xlarge: { fontSize: `${rems('36px')}` }
    }
};
